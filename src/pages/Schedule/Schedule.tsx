import React, { FC } from 'react';
import styles from './Schedule.module.scss';

const Schedule: FC = () => {
  return <section className={styles.schedule}>schedule</section>;
};

export default Schedule;
